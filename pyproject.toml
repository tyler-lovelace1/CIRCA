[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "circa"
version = "0.1.0"
description = "Causal Interaction Representations for Cellular Architectures"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]